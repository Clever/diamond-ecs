run:
  type: docker
env:
  #- HOSTNAME # passed through by starting with "host" docker network mode
  - ENVIRONMENT
  - GRAPHITE_HOST
  - GRAPHITE_PORT
  - SIGNALFX_TOKEN
resources:
  cpu: .25
  max_mem: .1
team: eng-infra
